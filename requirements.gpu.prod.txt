# GPU extras: Torch/CUDA берутся из образа pytorch/pytorch (см. dockerfile.gpu.prod).
# Локально: pip install -r requirements.prod.txt && pip install onnxruntime-gpu ultralytics

-r requirements.prod.txt
