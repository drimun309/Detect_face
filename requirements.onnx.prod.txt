# CPU extras: Torch CPU ставится в dockerfile.cpu.prod.
# Локально: pip install -r requirements.prod.txt && pip install onnxruntime ultralytics
#           pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

-r requirements.prod.txt
